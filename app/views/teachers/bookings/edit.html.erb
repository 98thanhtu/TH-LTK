<section style="margin-bottom:-100px">
  <div class="py-20 bg-gray-900 radius-for-skewed">
    <div class="container mx-auto px-4">
      <div class="mb-16 max-w-xl mx-auto text-center">
        <h2 class="text-5xl font-bold text-white">Hủy yêu cầu mua hàng</h2>
        <br>
      </div>
      <%= form_for ([:teachers, @booking]) do |form| %>
        <% if @booking.errors.any? %>
          <div id="error_explanation">
            <h2><%= pluralize(@booking.errors.count, "error") %> prohibited this noti from being saved:</h2>

            <ul>
              <% @booking.errors.each do |error| %>
                <li><%= error.full_message %></li>
              <% end %>
            </ul>
          </div>
        <% end %>
        <div class="field">
            <label class="text-white">Lý do hủy</label>
            <%= form.text_area :description, style:"width:100%" %>
          </div>

          <button class="btn btn-primary" type="submit">Đồng ý</button>
          <%= link_to 'Trở về', teachers_bookings_path, style:"color:white", :class => "btn btn-danger" %>
      <% end %>
    </div>
  </div>
</section>
